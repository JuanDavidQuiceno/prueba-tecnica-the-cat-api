import 'package:flutter/material.dart';

class ApiRequestModel extends StatelessWidget {
  const ApiRequestModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
